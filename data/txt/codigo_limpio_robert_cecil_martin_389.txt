j3: constantes frente a enumeraciones ahora que se han añadido enumeraciones al lenguaje (java 5), ¡úselas! no recurra al viejo truco de public static final int . el significado de int se puede perder. el de enum no, ya que pertenece a una enumeración con nombre. es más, analice atentamente la sintaxis de las enumeraciones . pueden tener métodos y campos, lo que las convierte en potentes herramientas que ofrecen mayor expresividad y flexibilidad que los int . fíjese en esta variante del código: public class hourlyemployee extends employee { private int tenthsworked; hourlypaygrade grade; public money calculatepay() { int straighttime = math.min(tenthsworked, tenths_per_week); int overtime = tenthsworked - straighttime; return new money( grade.rate() * (tenthsworked + overtime_rate * overtime) ); } … } public enum hourlypaygrade { apprentice { public double rate() { return 1.0; } }, leutenant_journeyman ( public double rate() { return 1.2; } }, journeyman { public double rate() { return 1.5; } }, master { public double rate() { return 2.0; } }; public abstract double rate(); 390